// Minimal target program for the cli_autoresearch example.
// The agent's job per iteration is to refine this file according
// to whatever program.md describes. Builds via CMake to ./build/hello.

#include <iostream>

int main() {
    std::cout << "hello from cli_autoresearch example\n";
    return 0;
}
